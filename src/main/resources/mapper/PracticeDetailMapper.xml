<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.graphic_reasoning_server.mapper.PracticeDetailMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_practice_detail
        (record_id, user_id, question_id, user_option, correct_option, is_correct, duration, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.recordId}, #{item.userId}, #{item.questionId},
            #{item.userOption}, #{item.correctOption}, #{item.isCorrect},
            #{item.duration}, #{item.createTime})
        </foreach>
    </insert>

</mapper>